export interface Coordinates {
    x: number;
    y: number;
    width: number;
    height: number;
}

export interface TextureSource {
    name: string;
    url: string;
}


export interface BlockPosition {
    dx: number;
    dy: number;
    dw: number;
    dh: number;
}